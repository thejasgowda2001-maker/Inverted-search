# Inverted-search
Implemented an Inverted Search system in C using hash tables and linked lists to efficiently index and retrieve words from multiple text files. Supports database creation, word search, database display, updating with additional files, and saving the indexed data to a file.
